<Window x:Class="SpaceVenueApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 codex/create-desktop-app-for-billiards-venue-pnjysf
        Title="Space Venue Manager" Height="920" Width="1480"

 HEAD
        Title="Space Venue Manager" Height="920" Width="1480"

        Title="Space Venue Manager" Height="900" Width="1400"
 main
 main
        Background="{StaticResource SpaceDarkBrush}"
        FlowDirection="{Binding UiFlowDirection}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border CornerRadius="16" Background="#15153A" Padding="20" Margin="0 0 0 20">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical">
          <TextBlock Text="Space Venue Control Center" FontSize="28" FontWeight="Bold" />
 codex/create-desktop-app-for-billiards-venue-pnjysf
          <TextBlock Text="Real-time timing, billing, cash, and reports (EGP)." FontSize="14" Opacity="0.8" />

 HEAD
          <TextBlock Text="Real-time timing, billing, cash, and reports (EGP)." FontSize="14" Opacity="0.8" />

          <TextBlock Text="Real-time timing, billing, and cash tracking (EGP)." FontSize="14" Opacity="0.8" />
 main
 main
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
          <TextBlock Text="Arabic RTL" VerticalAlignment="Center" />
          <ToggleButton IsChecked="{Binding IsRtlEnabled}" Width="56" Height="28">
            <ToggleButton.Template>
              <ControlTemplate TargetType="ToggleButton">
                <Border CornerRadius="14" Background="#2A2A4F" BorderThickness="1" BorderBrush="#3E3E6D">
                  <Grid>
                    <Ellipse x:Name="Switch" Fill="{StaticResource SpaceGoldBrush}" Width="20" Height="20" HorizontalAlignment="Left" Margin="4" />
                  </Grid>
                </Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Switch" Property="HorizontalAlignment" Value="Right" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </ToggleButton.Template>
          </ToggleButton>
        </StackPanel>
      </Grid>
    </Border>

 codex/create-desktop-app-for-billiards-venue-pnjysf

 HEAD
 main
    <TabControl Grid.Row="1" Background="#1B1B46" BorderThickness="0">
      <TabItem Header="Stations">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
          <StackPanel Spacing="16">
            <TextBlock Text="Live Stations" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
            <ItemsControl ItemsSource="{Binding Stations}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <WrapPanel Orientation="Horizontal" ItemWidth="360" ItemHeight="320" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border Background="#1F1F53" CornerRadius="16" Margin="0 0 16 16" Padding="16" Width="340" Height="300">
                    <Grid>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                      </Grid.RowDefinitions>
                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <Ellipse Width="12" Height="12" Fill="{Binding StatusBrush}" />
                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" />
                        <TextBlock Text="•" FontSize="16" Opacity="0.6" />
                        <TextBlock Text="{Binding Type}" FontSize="14" Opacity="0.7" />
                      </StackPanel>

                      <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0 10 0 0">
                        <TextBlock Text="Rate:" Opacity="0.7" />
                        <TextBlock FontWeight="SemiBold">
                          <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                              <Binding Path="RatePerHour" />
                              <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                            </MultiBinding>
                          </TextBlock.Text>
                        </TextBlock>
                      </StackPanel>

                      <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0 8 0 0">
                        <TextBlock Text="Customer" Opacity="0.7" />
                        <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Width="200" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                      </StackPanel>

                      <StackPanel Grid.Row="3" Orientation="Vertical" Spacing="6" Margin="0 8 0 0">
                        <TextBlock Text="Elapsed" Opacity="0.7" />
                        <TextBlock FontSize="18" FontWeight="Bold">
                          <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource TimeDisplayConverter}">
                              <Binding Path="ElapsedDisplay" />
                              <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                            </MultiBinding>
                          </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="Current charge" Opacity="0.7" />
                        <TextBlock FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}">
                          <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                              <Binding Path="CurrentCharge" />
                              <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                            </MultiBinding>
                          </TextBlock.Text>
                        </TextBlock>
                      </StackPanel>

                      <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                        <Button Content="Start" Command="{Binding StartCommand}" Padding="12 6" />
                        <Button Content="Pause" Command="{Binding PauseCommand}" Padding="12 6" />
                        <Button Content="Stop" Command="{Binding StopCommand}" Padding="12 6" />
                        <Button Content="Reset" Command="{Binding ResetCommand}" Padding="12 6" />
                      </StackPanel>
                    </Grid>
                  </Border>
 codex/create-desktop-app-for-billiards-venue-pnjysf

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column="0">
        <StackPanel Spacing="16">
          <TextBlock Text="Live Stations" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
          <ItemsControl ItemsSource="{Binding Stations}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" ItemWidth="360" ItemHeight="230" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Background="#1B1B46" CornerRadius="16" Margin="0 0 16 16" Padding="16" Width="340" Height="210">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="*" />
                      <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                      <Ellipse Width="12" Height="12" Fill="{Binding StatusBrush}" />
                      <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" />
                      <TextBlock Text="•" FontSize="16" Opacity="0.6" />
                      <TextBlock Text="{Binding Type}" FontSize="14" Opacity="0.7" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0 10 0 0">
                      <TextBlock Text="Rate:" Opacity="0.7" />
                      <TextBlock Text="{Binding RatePerHour, StringFormat={}{0:C}}" FontWeight="SemiBold" />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Vertical" Spacing="6" Margin="0 8 0 0">
                      <TextBlock Text="Elapsed" Opacity="0.7" />
                      <TextBlock Text="{Binding ElapsedDisplay}" FontSize="18" FontWeight="Bold" />
                      <TextBlock Text="Current charge" Opacity="0.7" />
                      <TextBlock Text="{Binding CurrentCharge, StringFormat={}{0:C}}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                    </StackPanel>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                      <Button Content="Start" Command="{Binding StartCommand}" Padding="12 6" />
                      <Button Content="Pause" Command="{Binding PauseCommand}" Padding="12 6" />
                      <Button Content="Stop" Command="{Binding StopCommand}" Padding="12 6" />
                    </StackPanel>
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </ScrollViewer>

      <StackPanel Grid.Column="1" Spacing="16">
        <Border Background="#1B1B46" CornerRadius="16" Padding="16">
          <StackPanel Spacing="12">
            <TextBlock Text="Cash Register" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Opening balance" Opacity="0.7" />
              <TextBlock Text="{Binding CashSummary.OpeningBalance, StringFormat={}{0:C}}" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Cash in" Opacity="0.7" />
              <TextBlock Text="{Binding CashSummary.TotalDeposits, StringFormat={}{0:C}}" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Cash out" Opacity="0.7" />
              <TextBlock Text="{Binding CashSummary.TotalWithdrawals, StringFormat={}{0:C}}" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Expected balance" Opacity="0.7" />
              <TextBlock Text="{Binding CashSummary.ExpectedClosing, StringFormat={}{0:C}}" FontWeight="SemiBold" Foreground="{StaticResource SpaceGoldBrush}" />
            </StackPanel>
          </StackPanel>
        </Border>

        <Border Background="#1B1B46" CornerRadius="16" Padding="16">
          <StackPanel Spacing="12">
            <TextBlock Text="Item Sales" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
            <ItemsControl ItemsSource="{Binding Items}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Spacing="8">
                    <TextBlock Text="{Binding Name}" Width="120" />
                    <TextBlock Text="{Binding Price, StringFormat={}{0:C}}" Width="100" />
                    <TextBlock Text="{Binding UnitsSold}" Width="60" />
                  </StackPanel>
 main
 main
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
 codex/create-desktop-app-for-billiards-venue-pnjysf

 HEAD
 main
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Items">
        <Grid Padding="16">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <Border Background="#1F1F53" CornerRadius="16" Padding="16" Margin="0 0 16 0">
            <StackPanel Spacing="12">
              <TextBlock Text="Item Catalog" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
              <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" Background="#15153A" BorderThickness="0" Height="520">
                <ListView.View>
                  <GridView>
                    <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Name}" Width="160" />
                    <GridViewColumn Header="Price" Width="100">
                      <GridViewColumn.CellTemplate>
                        <DataTemplate>
                          <TextBlock>
                            <TextBlock.Text>
                              <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                                <Binding Path="Price" />
                                <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                              </MultiBinding>
                            </TextBlock.Text>
                          </TextBlock>
                        </DataTemplate>
                      </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Units" DisplayMemberBinding="{Binding UnitsSold}" Width="80" />
                    <GridViewColumn Header="Revenue" Width="120">
                      <GridViewColumn.CellTemplate>
                        <DataTemplate>
                          <TextBlock>
                            <TextBlock.Text>
                              <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                                <Binding Path="Revenue" />
                                <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                              </MultiBinding>
                            </TextBlock.Text>
                          </TextBlock>
                        </DataTemplate>
                      </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                  </GridView>
                </ListView.View>
              </ListView>
            </StackPanel>
          </Border>

          <StackPanel Grid.Column="1" Spacing="16">
            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Manage Item" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <TextBlock Text="Name" Opacity="0.7" />
                <TextBox Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                <TextBlock Text="Price (EGP)" Opacity="0.7" />
                <TextBox Text="{Binding NewItemPrice, UpdateSourceTrigger=PropertyChanged}" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <Button Content="Add" Command="{Binding AddItemCommand}" Padding="12 6" />
                  <Button Content="Update" Command="{Binding UpdateItemCommand}" Padding="12 6" />
                  <Button Content="Delete" Command="{Binding DeleteItemCommand}" Padding="12 6" />
                </StackPanel>
              </StackPanel>
            </Border>

            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Record Sale" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <TextBlock Text="Quantity" Opacity="0.7" />
                <TextBox Text="{Binding SaleQuantity, UpdateSourceTrigger=PropertyChanged}" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                <Button Content="Add Sale" Command="{Binding RecordSaleCommand}" Padding="12 6" HorizontalAlignment="Left" />
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>
      </TabItem>

      <TabItem Header="Cash Register">
        <Grid Padding="16">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <Border Background="#1F1F53" CornerRadius="16" Padding="16" Margin="0 0 16 0">
            <StackPanel Spacing="12">
              <TextBlock Text="Cash Transactions" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
              <ListView ItemsSource="{Binding CashTransactions}" Background="#15153A" BorderThickness="0" Height="520">
                <ListView.View>
                  <GridView>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="100" />
                    <GridViewColumn Header="Amount" Width="120">
                      <GridViewColumn.CellTemplate>
                        <DataTemplate>
                          <TextBlock>
                            <TextBlock.Text>
                              <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                                <Binding Path="Amount" />
                                <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                              </MultiBinding>
                            </TextBlock.Text>
                          </TextBlock>
                        </DataTemplate>
                      </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Notes" DisplayMemberBinding="{Binding Notes}" Width="180" />
                  </GridView>
                </ListView.View>
              </ListView>
            </StackPanel>
          </Border>

          <StackPanel Grid.Column="1" Spacing="16">
            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Cash Summary" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Opening" Opacity="0.7" />
                  <TextBlock>
                    <TextBlock.Text>
                      <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                        <Binding Path="CashSummary.OpeningBalance" />
                        <Binding Path="IsRtlEnabled" />
                      </MultiBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Deposits" Opacity="0.7" />
                  <TextBlock>
                    <TextBlock.Text>
                      <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                        <Binding Path="CashSummary.TotalDeposits" />
                        <Binding Path="IsRtlEnabled" />
                      </MultiBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Withdrawals" Opacity="0.7" />
                  <TextBlock>
                    <TextBlock.Text>
                      <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                        <Binding Path="CashSummary.TotalWithdrawals" />
                        <Binding Path="IsRtlEnabled" />
                      </MultiBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Expected" Opacity="0.7" />
                  <TextBlock Foreground="{StaticResource SpaceGoldBrush}">
                    <TextBlock.Text>
                      <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                        <Binding Path="CashSummary.ExpectedClosing" />
                        <Binding Path="IsRtlEnabled" />
                      </MultiBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </StackPanel>
              </StackPanel>
            </Border>

            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Add Transaction" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <TextBlock Text="Amount" Opacity="0.7" />
                <TextBox Text="{Binding TransactionAmount, UpdateSourceTrigger=PropertyChanged}" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                <TextBlock Text="Notes" Opacity="0.7" />
                <TextBox Text="{Binding TransactionNotes, UpdateSourceTrigger=PropertyChanged}" Background="#2A2A5E" Foreground="White" BorderBrush="#3E3E6D" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <Button Content="Deposit" Command="{Binding DepositCommand}" Padding="12 6" />
                  <Button Content="Withdraw" Command="{Binding WithdrawCommand}" Padding="12 6" />
                </StackPanel>
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>
      </TabItem>

      <TabItem Header="Reports">
        <Grid Padding="16">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <Border Background="#1F1F53" CornerRadius="16" Padding="16" Margin="0 0 16 0">
            <StackPanel Spacing="12">
              <TextBlock Text="Sessions (Today)" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
              <ListView ItemsSource="{Binding Sessions}" Background="#15153A" BorderThickness="0" Height="520">
                <ListView.View>
                  <GridView>
                    <GridViewColumn Header="Station" DisplayMemberBinding="{Binding StationName}" Width="140" />
                    <GridViewColumn Header="Customer" DisplayMemberBinding="{Binding CustomerName}" Width="120" />
                    <GridViewColumn Header="Start" DisplayMemberBinding="{Binding StartTime}" Width="160" />
                    <GridViewColumn Header="End" DisplayMemberBinding="{Binding EndTime}" Width="160" />
                    <GridViewColumn Header="Cost" Width="100">
                      <GridViewColumn.CellTemplate>
                        <DataTemplate>
                          <TextBlock>
                            <TextBlock.Text>
                              <MultiBinding Converter="{StaticResource CurrencyDisplayConverter}">
                                <Binding Path="Cost" />
                                <Binding Path="DataContext.IsRtlEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                              </MultiBinding>
                            </TextBlock.Text>
                          </TextBlock>
                        </DataTemplate>
                      </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                  </GridView>
                </ListView.View>
              </ListView>
            </StackPanel>
          </Border>

          <StackPanel Grid.Column="1" Spacing="16">
            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Daily Statement" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <TextBlock Text="{Binding DailyReportSummary}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
            <Border Background="#1F1F53" CornerRadius="16" Padding="16">
              <StackPanel Spacing="12">
                <TextBlock Text="Monthly Statement" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
                <TextBlock Text="{Binding MonthlyReportSummary}" TextWrapping="Wrap" />
                <Button Content="Export CSV" HorizontalAlignment="Right" Padding="12 6" Command="{Binding ExportCommand}" />
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>
      </TabItem>
    </TabControl>
 codex/create-desktop-app-for-billiards-venue-pnjysf


        </Border>

        <Border Background="#1B1B46" CornerRadius="16" Padding="16">
          <StackPanel Spacing="12">
            <TextBlock Text="Daily Statement" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SpaceGoldBrush}" />
            <TextBlock Text="{Binding DailyReportSummary}" TextWrapping="Wrap" />
            <Button Content="Export" HorizontalAlignment="Right" Padding="12 6" Command="{Binding ExportCommand}" />
          </StackPanel>
        </Border>
      </StackPanel>
    </Grid>
 main
 main
  </Grid>
</Window>
